import { TrackConnectorFactory } from './track-connector-factory';

describe('TrackConnectorFactory', () => {
  it('should create an instance', () => {
    expect(new TrackConnectorFactory()).toBeTruthy();
  });
});
